import { afterEach,describe,expect,it,vi } from "vitest";
import { api,qs } from "./client";
describe("api client",()=>{afterEach(()=>vi.restoreAllMocks());it("does not trust a caller-provided tenant and adds idempotency headers",async()=>{const fetchMock=vi.spyOn(globalThis,"fetch").mockResolvedValue(new Response(JSON.stringify({id:"1"}),{status:200,headers:{"Content-Type":"application/json"}}));await api("/bookings",{tenantId:"tenant",idempotencyKey:"key"});const headers=(fetchMock.mock.calls[0]?.[1]?.headers) as Headers;expect(headers.get("X-Tenant-Id")).toBeNull();expect(headers.get("Idempotency-Key")).toBe("key")});it("parses Problem Details",async()=>{vi.spyOn(globalThis,"fetch").mockResolvedValue(new Response(JSON.stringify({title:"Conflicto",status:409,detail:"Ocupado"}),{status:409,headers:{"Content-Type":"application/problem+json","X-Correlation-ID":"corr-1"}}));await expect(api("/bookings")).rejects.toMatchObject({problem:{status:409,correlationId:"corr-1"}})});it("builds query strings without empty values",()=>expect(qs({date:"2026-08-21",resourceId:undefined})).toBe("date=2026-08-21"))});
